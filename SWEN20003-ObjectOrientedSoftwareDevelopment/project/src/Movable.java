/**
 * this is an interface for character's moving
 */
public interface Movable {
    /**
     * moving of character
     * @param x moving step on x-position
     * @param y moving step on y-position
     */
    void move(double x, double y);
}
